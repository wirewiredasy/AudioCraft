Perfect! Tumne ek pro-level frontend tech stack decide kiya hai — ab main tumhe ODOREMOVER ke liye ek complete, beautiful, modern React + Vite + Tailwind + Radix UI frontend layout dunga (Fully structured + all sections) jo:

Upload system ke liye optimized hoga

Real-time response dikha sakega

Dark mode, drag-drop, animation sab included

Production-level ready rahega future ke liye (Pro feel, no boring white page)



---

✅ Full Website Structure — ODOREMOVER

🔧 Tech Stack (with install commands)

# Project setup
npm create vite@latest odoremover -- --template react-ts
cd odoremover

# Dependencies install
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# UI & animations
npm install clsx framer-motion @radix-ui/react-tabs @radix-ui/react-switch @shadcn/ui

# Upload & form
npm install react-dropzone react-hook-form zod

# Notification
npm install sonner

# API call
npm install axios


---

🧠 Folder Structure

odoremover/
├── src/
│   ├── components/
│   │   ├── Header.tsx
│   │   ├── UploadBox.tsx
│   │   ├── ResultPlayer.tsx
│   │   ├── ModeToggle.tsx
│   │   └── Footer.tsx
│   ├── pages/
│   │   └── Home.tsx
│   ├── App.tsx
│   ├── main.tsx
│   ├── index.css
│   └── lib/
│       └── utils.ts
├── public/
│   └── og-image.png
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts


---

🌟 UI Design Overview (Components)

1. Header.tsx

Logo: ODOREMOVER

Navigation (Home, Tools, Docs)

Dark mode toggle

Clean transparent style



---

2. UploadBox.tsx

Drag and drop (React Dropzone)

Upload button

Progress bar

Auto-detect file type (WAV/MP3)

Animation (Framer Motion)



---

3. ResultPlayer.tsx

After backend response

Show audio player with waveform (or just audio tag)

Tabs: Original | Removed Vocals

Download button for both



---

4. ModeToggle.tsx

Dark/light toggle with animation (Radix Switch)



---

5. Footer.tsx

Copyright

Tool links

GitHub or About



---

🎨 UI Layout Structure (Page)

// App.tsx
import { Home } from './pages/Home'
import { Toaster } from 'sonner'
import './index.css'

function App() {
  return (
    <>
      <Home />
      <Toaster richColors position="top-center" />
    </>
  )
}

export default App

// Home.tsx
import { Header } from '@/components/Header'
import { UploadBox } from '@/components/UploadBox'
import { ResultPlayer } from '@/components/ResultPlayer'
import { ModeToggle } from '@/components/ModeToggle'
import { Footer } from '@/components/Footer'

export const Home = () => {
  return (
    <div className="min-h-screen bg-background text-foreground transition-colors">
      <Header />
      <main className="max-w-4xl mx-auto px-4 py-12 flex flex-col gap-8">
        <h1 className="text-4xl font-bold text-center">ODOREMOVER</h1>
        <UploadBox />
        <ResultPlayer />
      </main>
      <Footer />
    </div>
  )
}


---

✨ Tailwind Setup Snippet (tailwind.config.js)

module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        background: "#0f0f0f",
        foreground: "#ffffff",
        card: "#1c1c1c",
        accent: "#4ade80",
      }
    },
  },
  plugins: [],
}


---

⚡ Sonner Setup (Notification)

import { toast } from 'sonner'

toast.success("File uploaded successfully")
toast.error("Something went wrong")


---

🔮 Bonus Tips

✅ Add file type check using Zod

✅ Use axios to POST file to backend

✅ You can add waveform player later like wavesurfer.js

✅ Use skeleton loading for processing status



---

🔄 Replit Prompt for OCR UI Clone (if image hai)

Design same to same as this UI:
- Use React + Tailwind + Radix
- Add Upload zone with drag-drop (Dropzone)
- Audio player result section with tabs (Radix Tabs)
- Include Mode toggle (Dark/Light)
- Animated transitions (Framer Motion)
- Notification using Sonner
- Layout: Grid based, Pro style
- Colors: Dark background, accent green, no whitewash


---

Agar chaho toh main ye sab code Replit ke liye main.jsx, components, vite.config.js ke sath complete bana ke de sakta hoon. Batao live setup chahiye ya export code chahiye?

✅ Ye sab karne ke baad ODOREMOVER professional tool ban jayega like VocalRemover + BandLab fusion.

Batao ab kis part se start karein — Header ya UploadBox?

